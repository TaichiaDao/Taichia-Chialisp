; This is a "limitations_program" for use with cat.clvm.
;
(mod (
      PUBKEY
      Truths
      parent_is_cat
      lineage_proof
      delta
      inner_conditions
      (
        gateway_id
        fund_amount
        mint_amount
      )
    )

    (include condition_codes.clib)
    (list (list AGG_SIG_UNSAFE PUBKEY (sha256 gateway_id fund_amount mint_amount)))
)
